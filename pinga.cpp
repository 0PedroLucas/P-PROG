#include<stdio.h>
#include<locale.h>
#include<stdlib.h>

int main(){
	int i, tab, qntd;
	printf("Digite o numero da tabuada que deseja ver:");
	scanf("%i", &tab);
	for(i= 1 ; i <=10 ; i++){
		printf("%i X %i= %i\n", tab, i, tab*i);
		
	}
	printf("Digite a quantidade de vezes que a mensagem deve aparecer:");
	scanf("%i", &qntd);
	
for(i=0; i<qntd; i++){
printf("Curso eng. eletrica\n");
  }
}
